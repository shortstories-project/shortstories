<%= render partial: 'shared/breadcrumbs' %>

<div class="container-fluid">
  <h1>Story № <%= story.id %></h1>

  <p>
    <b>text: </b><%= story.text %>
  </p>
  <p>
    <b>state: </b><%= story.state %>
  </p>
  <p>
    <b>Created at: </b><%= story.created_at.to_date %>
  </p>

  <div>
    <%= cmd(story, 'approved', class_name: 'btn btn-success') %>
    <%= cmd(story, 'declined', class_name: 'btn btn-danger') %>
  </div>
</div>
