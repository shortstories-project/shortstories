import * as React from 'react'
import * as ReactDOM from 'react-dom'
import AppWithHotLoader from './container'

const root = document.getElementById('app')

ReactDOM.render(<AppWithHotLoader />, root)
